package com.muzzley.model.profiles

enum class RecipeAction {
    show_info, oauth, list_devices, send_selected_devices,
    udp, convert_for_show, show_devices, grant_access_user, grant_access_app
}
